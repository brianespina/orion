# Endpoint the contact form POSTs JSON to (Formspree, a Worker, an API route, …).
# Payload: { firstName, lastName, email, company, message }
#
# Leave unset and the form falls back to composing the message in the visitor's
# own mail client via a mailto: link.
PUBLIC_CONTACT_ENDPOINT=
