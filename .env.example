# No environment variables are required to build or run this site.
#
# The contact form posts to Web3Forms. Its access key is a public identifier
# that ships in the page markup, so it lives in `src/data/site.ts` rather than
# here — swap it there to point submissions at a different Web3Forms inbox.
